'use strict';
/**
 * currentLocation styles
 * @flow
 */

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'flex-start',
    marginTop: 70,
  },
  heading: {
    textAlign: 'center',
    color: '#000000',
    fontWeight: '900',
    marginBottom: 70,
    fontSize: 22
  }
});
