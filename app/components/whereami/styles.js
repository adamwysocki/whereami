'user strict';
/**
 * whereami component styles
 * @flow
 *
 */

 import { StyleSheet } from 'react-native';

 export default StyleSheet.create({
   container: {
     flex: 1,
     marginTop: 20,
     justifyContent: 'center',
     alignItems: 'center',
     backgroundColor: '#FFFFFF',
   },
   title: {
     color: '#ffffff',
     fontWeight: '900',
     marginTop: 10,
     fontSize: 18
   }
 });
